export const SearchBar = () => {
  return (
    <form>
      <label htmlFor="search"></label>
      <input
        type="search"
        name="search"
        id="search"
        placeholder="Type your search"
      ></input>
    </form>
  );
};
